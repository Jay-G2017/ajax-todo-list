= form_for task do |f|
  = f.check_box :complete
  = f.label :complete, task.name
  = link_to "(remove!)", task, method: :delete, remote: true, data: {confirm: "Are you sure?"}
  = link_to "(edit)", edit_task_path(task), remote: true
  
